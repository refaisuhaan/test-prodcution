name: CI
<<<<<<< HEAD

on:
  push:
    branches: [ main ]
 

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4


            - name: Login to DockerHub
              uses: docker/login-action@v2
              with:
                username: ${{ secrets.DOCKER_USERNAME }}
                password: ${{ secrets.DOCKER_PASSWORD }}

            - name: Generate timestamp tag
              run: echo "VERSION=$(date +%Y%m%d%H%M%S)" >> $GITHUB_ENV

            - name: Build and push Docker image
                
              run: |
                docker build -t ${{ secrets.DOCKER_USERNAME }}/flask-APP:${{ env.VERSION }} .
                docker push ${{ secrets.DOCKER_USERNAME }}/flask-APP:${{ env.VERSION }}
=======
on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Generate timestamp tag
        run: echo "VERSION=$(date +%Y%m%d%H%M%S)" >> "$GITHUB_ENV"

      - name: Debug secret length
        run: echo "Username length is ${#DOCKER_USERNAME}"
        env:
         DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/helloworld:${{ env.VERSION }}
>>>>>>> 02f7915e39a0597cba1164751fcb7d352769ecd4
